╔════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║ is a task performed only when the User mandates a human-centric reflection.",                              ║
║       "versioning_archival": "When updating Markdown documents, always rename the previous version and move it to the║
║ local context/ directory (e.g., <AGENT>_<SESSION>_<NAME>_v<OLD>.md) before establishing the new version in the primary║
║ md/ location.",                                                                                            ║
║       "relay_takeover_mandate": "MANDATE: When instantiating a WeDo from a relay (taking over an open WeDo from a dead║
║ agent), you MUST include a line in the new WeDo instance (e.g., in the 'meta' or 'rules' section) stating the ease of║
║ takeover and the duration required to assume the role.",                                                   ║
║       "direct_aesthetic": "Present reports 'straightup' without backtick wrappers.",                       ║
║       "rna_presumption": "All raw session artifacts (WeDo instances, logs, intermediate reports) are considered 'RNA'║
║ and MUST reside in '.hidden/' subdirectories of their respective temporal or leaf folders.",               ║
║       "heartwood_expression": "Only refined artifacts, approved for permanent inclusion by the user, are promoted from║
║ '.hidden/' to the parent leaf folder (e.g., md/ or json/) as 'DNA' (Heartwood).",                          ║
║       "aspirational_filesystem": "The filesystem structure reflects the agent's aspirations: '.hidden/' for process║
╚════════════════════════════════════════════════════════════════════════════════════════════════════════════╝
