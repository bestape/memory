╔════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║     entries) MUST occur within the temporal directory of the image's creation date. Do NOT use the current session's║
║     date for historical artifacts.",                                                                       ║
║  61 -     "daily_index_reset": "MANDATE: The IMG index (e.g., IMG-1, IMG-2) MUST reset to 1 at the beginning of every║
║     new calendar day (chrono-fractal). Global continuous numbering is strictly prohibited.",               ║
║  62 -     "creation_date_locality": "MANDATE: Artifacts MUST be stored in the directory corresponding to their║
║     creation timestamp, NOT the current session date.",                                                    ║
║  63 -     "active_investigation": "Verify existence of artifacts on localhost before inclusion.",          ║
║  64 -     "dual_referencing": "Reference both localhost paths and remotehost URLs.",                       ║
║  65 -     "relay_takeover_mandate": "MANDATE: When instantiating a WeDo from a relay (taking over an open WeDo from a║
║     dead agent), you MUST include a line in the new WeDo instance (e.g., in the 'meta' or 'rules' section) stating the║
║     ease of takeover and the duration required to assume the role.",                                       ║
║  66 -     "chronological_integrity": "ALWAYS append new entries to the absolute end of the file. Use the 'replace'║
║     tool with the last few lines of the file as context to ensure no out-of-order dangling entries remain.",║
╚════════════════════════════════════════════════════════════════════════════════════════════════════════════╝
