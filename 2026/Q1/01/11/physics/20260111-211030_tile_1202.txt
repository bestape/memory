╔════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║ without relying on a centralized database.",                                                               ║
║       "layer_2_the_402_handshake": "The x402 protocol functions as the network-layer gateway. When an Agent (Client A)║
║ requests a node from the Gateway (Server B), the flow is as follows: 1. Client A sends GET /api/7827/part-source. 2.║
║ Server B identifies the resource as protected and returns 'HTTP 402 Payment Required'. 3. The response body contains a║
║ JSON payload: { 'amount': '0.01', 'token': 'USDC', 'address': '0xd24fD549...AcDD', 'chain': 'sepolia' }. 4. Client A's║
║ internal wallet (the 'managed_wallet' we just instantiated) detects the 402 and programmatically executes a transfer.║
║ 5. Client A retries the request with the transaction hash in the 'Authorization' header.",                 ║
║       "layer_3_verification_and_unlock": "The Gateway monitors the Sepolia chain for the provided tx_hash. Once the║
║ transfer is confirmed, it serves the requested JSON node from the ERC-7827 contract. For 'write' access, the Gateway║
║ provides a signed 'Permission Strike' that the client must include in the on-chain 'write' transaction to pass the║
║ 'onlySigner' or similar access control logic."                                                             ║
║     },                                                                                                     ║
║     "agent_specific_implementations": {                                                                    ║
╚════════════════════════════════════════════════════════════════════════════════════════════════════════════╝
